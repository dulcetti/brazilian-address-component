import BrazilianAddressComponent from './brazilian-address-component';

describe('Brazilian Address Component', () => {
  test('if component should be defined', () => {
    expect(BrazilianAddressComponent).toBeDefined();
  });
});
